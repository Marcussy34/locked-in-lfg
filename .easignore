# Dungeon — served from Vercel in production, not needed in APK
web/

# Raw 3D source assets — not used in the app
models/
textures/

# Backend — not needed in mobile build
backend/

# iOS — not needed for Android build
ios/

# Other non-build files
.git/
docs/
